import { Factory, Fingerprint, Image } from "@mui/icons-material";

const propIdToIcon = {
    image: Image,
    manufacturer: Factory,
    manufacturer_part_num: Fingerprint,
};

export { propIdToIcon };
